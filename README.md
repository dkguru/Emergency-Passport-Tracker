# Emergency Passport Tracker

A self-contained windows application that:

* *Runs locally (user space)
* *Stores data alongside the executable
* *Encrypts the data file with a PIN
* Allows adding ranges of passport numbers
* Prevents deletion (append-only)
* Includes print functionality that recreates the structure of the word log form

